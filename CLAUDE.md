# CLAUDE.md — whizzzest.com 多会话协作铁律

> 本仓库常态多个 Claude 会话在**同一工作区**并行作业（收录批次 / 修 bug / 方案文档并行）。任务声明与独占资源登记统一在 [docs/任务看板.md](./docs/任务看板.md)，本文只列铁律。

## 开工序（每次开工必做）

1. `git fetch` + `git status`：未提交改动先辨认归属——**不是你任务的文件一律不碰、不 add、不还原**；
2. 读 [docs/任务看板.md](./docs/任务看板.md)：登记任务名、文件范围、需要的独占资源；他人登记了相同范围/资源 → 先协调再动手；
3. 改任何文件前以磁盘现状重读（其他会话可能刚改过它），编辑工具报「文件已变更」时以磁盘为准。

## 编辑与提交

- 只改看板登记范围内的文件；共享文件（`docs/上线记录.md`、`game/public/games.json`、`games.en.json`、看板本身）做**增量编辑**（锚点插入/字段修改），禁止拿旧上下文整文件重写；
- 只 `git add <本任务文件清单>`；**禁止 `git add -A` / `git add .`**；提交前 `git status` 核对暂存区没有他人文件；
- 一个验收点 = 一个 commit + 一次 push；**已部署到线上的内容 30 分钟内必须落 commit 并 push**——压缩携带式提交（先例 31ec277）与 rebase 冲突的窗口；
- 提交说明沿仓库惯例：`类型(域): 摘要——细节；验收；部署 id`；提交携他人成稿内容时在说明里注明范围与归属（先例 8f6a987「代码文件归各批次自行提交」）。

## 部署（最锋利的共享资源）

- 部署是**独占资源**：看板上他人登记 wrangler deploy / upload-games.mjs 运行中 → 不得并发部署、不得改 `games*.json`；等登记撤销再继续；
- 部署前三查：① `git status` 确认没有他人半成品会被本次部署夹带；② `node game/scripts/check-i18n.mjs` 全绿；③ 主站文件有改动时 `node build.js` 校验通过；
- 部署后核对 wrangler 输出的上传/变更清单 = 预期增量；**出现预期外文件立即停手**，在看板挂事故记录再排查。

## 冲突善后

- 编辑失败 / 文件在磁盘上变了 → 重读文件、把你的编辑在新版上重放；**不要用记忆里的旧内容整文件覆盖**；
- push 被拒 → `git pull --rebase` 后重推；共享文件冲突**保留双方内容**（行级合并），不二选一；
- 出了事故（覆盖他人改动 / 部署夹带 / 交接断层）→ 修完在看板「冲突事故复盘」补记根因与改进，不只修不记。

## 工作模式（本仓库既定）

- 分析/方案先行，不碰业务代码；方案经业主认可后写入 `docs/` 对应方案文档；
- 收录/改码动手前先 `git fetch`（仓库远端常领先）；上游文档关系与既定决策见 `docs/` 各方案文档头部。
