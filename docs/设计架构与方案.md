# 焰境·万载 网站重构与迁移 —— 设计架构与实施方案（原生版）

> 文档版本：**v2.1** ｜ 2026-09-05 ｜ 状态：技术路线、域名、仓库已定，表单待定
> v2.1 变更：**域名决策 → 使用 whizzzest.com 主域**；新增「旧站弊病诊断与重构对策」（打开思路）；仓库确定为 whizzzest.com
> v1.0（Nuxt 4 框架版）已废弃 —— 用户决定：**不用框架，原生 HTML/CSS/JS**，理由：框架写起来重且复杂
> 依据：旧站线上实抓、Cloudflare 账号实连（OAuth 已授权）、阿里云企业邮 DNS 记录（截图核验）

---

## 1. 现状盘点（实查，未变）

### 1.1 旧站诊断

| 项目 | 现状 | 来源 |
|---|---|---|
| 站点名称 | 焰境·万载（"一朝相逢，便是万载"） | 首页实抓 |
| 技术栈 | Vue 3 + Vite SPA（将被替换） | HTML 源码 |
| 托管 | 阿里云 ECS（8.137.162.230，nginx 1.20.1） | DNS A 记录 + 响应头 |
| 域名 DNS | whizzzest-yanjingwanzai.top 的 NS = 阿里云万网（hichina） | NS 查询 |
| 页面形态 | 单页 SPA + 客户端路由 + 英文版 /en | 路由探测 |

### 1.2 旧站内容板块清单（迁移素材清单）

1. 首屏 Hero：一朝相逢，便是万载 / 探索千年烟花文化·非遗·美食·风光 / CTA「查看旅游线路」
2. 核心特色 ×3：千年烟花文化（1400+ 年，中国花炮之乡）、特色美食（六大碗/百合/扎粉）、旅游资源（古城/竹山洞/九龙原始森林）
3. 非遗文化：万载花炮、得胜鼓、万载开口傩、夏布织造
4. 美食特产：六大碗、剁肉、扎粉、百合粉、南酸枣糕
5. 烟花产业：中国四大花炮主产区之一
6. 主题曲：《有一个地方叫万载》（左湖海音乐室，3:12）
7. 数字烟花体验：在线放烟花（类型/特效/音效/全屏）
8. 关于 / 友情链接（县政府、万载发布抖音、中国烟花爆竹协会）
9. 联系我们：whizzzest@outlook.com、GitHub/Gitee、抖音
10. 公众号二维码 ×3：万载人民政府 / 万载文旅 / 焰境·万载

**图片资源**（10+ 张）：烟花夜景 hero、古城雪景、三十把水库、仙源红色研学、傩舞、六大碗、龙湖烟花晚会、中国万载 logo、3 张二维码。均在旧站 `/optimized/`、`/assets/` 下，可下载复用。

### 1.3 Cloudflare 账号现状（已 OAuth 连接）

| 项目 | 状态 |
|---|---|
| wrangler CLI | 4.129.0 已全局安装，OAuth 登录成功（nathanpenny@qq.com） |
| 域名 Zone | whizzzest.com（Free，active）在账号内 |
| Pages 项目 | 暂无（全新起步） |
| whizzzest.com DNS | 9 条阿里云企业邮记录（imap/mail/pop3/smtp CNAME、MX×3、SPF、DMARC） |

### 1.4 GitHub（已连接）

- SSH 认证通过（账号 nathanpenny520），仓库 `whizzzest.com` 已克隆到 `E:\Projects\website-group\whizzzest\whizzzest.com`（空仓库，默认分支 main）

### 1.5 本地工具链

- Node v22.23.2 / pnpm 11.8.0 / git 2.52.0 / wrangler 4.129.0 ✅（原生方案只需 Node 跑 build.js，pnpm 仅可选）

### 1.6 旧站弊病诊断与重构对策（打开思路，核心章节）

> 用户要求：之前网站弊病多，这次重构**全部解决、不被旧结构束缚**。下表逐条列弊病 → 对策 → 对应新方案落点。

| # | 旧站弊病（实抓/实测） | 重构对策 | 新方案落点 |
|---|---|---|---|
| 1 | **SPA 客户端渲染**：首屏白屏、搜索引擎抓不到内容 | 改为多页静态 HTML，首屏即完整内容 | §4 原生多页 |
| 2 | **单页长滚动**：所有板块堆在首页，信息密度过高 | 按主题拆 6 个独立页 + 清晰导航层级 | §6 页面规划 |
| 3 | **框架 JS 体积大**：Vue 全家桶分包，移动端加载慢 | 零框架运行时，JS 按功能拆文件、按需加载 | §4 技术选型 |
| 4 | **SEO 基建缺失**：无 sitemap/robots/结构化数据/规范 OG | build.js 自动生成 sitemap.xml、robots.txt、每页 canonical + JSON-LD（LocalBusiness/TouristAttraction） | §5 结构 / P6 |
| 5 | **图片无响应式**：固定尺寸、无 srcset、无懒加载 | srcset/sizes + loading="lazy" + 首屏关键图 preload | §6 交互模块 |
| 6 | **无版本管理/无 CI**：部署靠手工传 ECS，改坏了难回滚 | Git 全量管理 + Pages 自动部署 + 分支预览 + 一键回滚 | §9 流水线 |
| 7 | **无安全头**：缺 CSP/X-Frame-Options/Referrer-Policy | Pages `_headers` + Cloudflare 安全规则 | P5 |
| 8 | **无性能监控**：好坏无量化 | Cloudflare Web Analytics + 性能预算（LCP<2.5s，首屏 JS<30KB） | P6 |
| 9 | **品牌散乱**：.top 域名 + outlook 杂邮箱 | 统一 **whizzzest.com** 主域 + 阿里云企业邮（已配好） | §8 已定 |
| 10 | **无障碍缺失**：无 ARIA/键盘导航/对比度把控 | 语义化标签 + ARIA + 键盘可达 + 对比度 ≥4.5:1 | P3 规范 |
| 11 | **移动端体验粗糙**：无 PWA 基础、无离线能力 | manifest + 手写 service worker（约 50 行，缓存静态壳） | P3 可选 |
| 12 | **主题曲/素材外链脆弱**：音频依赖旧站 JS 拼接 | 素材全部下载到仓库静态托管，页面零外链 | P2 迁移 |

> 附加思路（打开设计）：
> - **信息架构**不再复制旧站板块顺序，按访客动线重排：游客（玩什么）→ 吃货（吃什么）→ 文化控（看什么）→ 商务（烟花产业）。
> - **视觉**不再沿用旧站默认模板感，用「焰色」品牌体系：深空黑底 + 焰火金/红渐变 + 大图大留白；烟花粒子交互作全站记忆点。
> - **英文版**不是"翻译稿"，而是为海外游客/采购商独立撰写的英文内容（烟花外贸是真实场景）。

---

## 2. 核心设计原则（为什么选原生）

| 原则 | 说明 |
|---|---|
| **零框架运行时** | 页面就是纯 HTML/CSS/JS，浏览器直接跑，无 Vue/React/Nuxt 任何一层 |
| **零依赖构建** | build.js 只用 Node 内置 fs/path，不装任何 npm 包，无 lockfile、无 node_modules |
| **零 EOL 风险** | 没有框架版本迭代问题（刚经历 Nuxt 3 EOL），文件 10 年后照样能打开 |
| **SEO 天然最优** | 每个页面是完整 HTML，搜索引擎直接收录，无 SPA 空白首屏 |
| **全掌控** | 每行代码自己写的，没有"魔法"，出问题可全链路排查 |
| **部署最简** | dist/ 静态目录直接上 Cloudflare Pages，构建命令就是 `node build.js` |

---

## 3. 目标架构

```
                        ┌──────────────────────┐
                        │  用户：浏览器 / 手机  │
                        └──────────┬───────────┘
                                   │ HTTPS
        ┌──────────────────────────▼──────────────────────────┐
        │      Cloudflare 全球网络（Anycast 边缘）             │
        │      CDN 静态缓存 · 自动 SSL · WAF · DDoS 防护       │
        │      自定义域名（决策点 A/B，见 §8）                  │
        ├──────────────────────────────────────────────────────┤
        │      Cloudflare Pages —— 纯静态托管                  │
        │      · dist/ 下的 HTML/CSS/JS 多页静态文件            │
        │      · 每页完整 HTML（SEO 全量收录）                  │
        │      · 图片/字体/音频智能缓存                         │
        ├──────────────────────────────────────────────────────┤
        │      Pages Functions —— 唯一的"后端"（原生 JS 文件）  │
        │      · POST /api/contact  联系表单 → 阿里云企业邮     │
        │      · POST /api/visit    访问计数（KV，可选）        │
        ├──────────────────────────────────────────────────────┤
        │      数据层（可选，免费额度内）                       │
        │      · D1：表单/留言记录（SQLite）                    │
        │      · KV：计数、配置                                 │
        └──────────────────────────────────────────────────────┘
        外部依赖：
        · 阿里云企业邮（whizzzest.com 域名邮箱）→ 表单通知收信
        · GitHub 仓库 → 源码版本管理 + Pages Git 部署
        · 公众号二维码 ×3 / 抖音 / 政府官网友情链接
```

> 注意：Pages Functions 是一个**原生 JS 文件**，不依赖任何框架，不属于"框架"范畴。

---

## 4. 技术选型（原生版）

| 层 | 选型 | 说明 |
|---|---|---|
| HTML | 手写语义化多页静态页 | 每页一个完整 .html，含独立 title/meta/canonical |
| CSS | 手写，CSS 变量 + Flex/Grid 响应式 | 单一 style.css（约 500-800 行），主题色沿用 #dc2626 |
| JS | 原生 ES Modules | 按功能拆文件：main.js（导航/滚动）、fireworks.js（数字烟花 Canvas）、player.js（音频）、contact.js（表单） |
| 构建 | **build.js**（Node 内置 fs，零依赖，约 50 行） | 拼接公共片段（header/footer/head）+ 替换 meta 占位符 → 输出 dist/ |
| 静态托管 | Cloudflare Pages | 构建命令 `node build.js`，输出目录 dist/ |
| 边缘函数 | Pages Functions（functions/api/*.js） | 原生 JS，处理表单与计数 |
| 数据库 | D1（可选） | 表单记录；Free 5GB |
| 邮件 | 阿里云企业邮（已有） | 表单通知发到现有邮箱 |
| 分析 | Cloudflare Web Analytics | 免费、免 Cookie 弹窗 |
| 版本管理 | GitHub `whizzzest.com`（已连接） | main 分支自动部署 |

**刻意不用的东西**：Vue、React、Nuxt、Tailwind、任何 npm 运行时依赖。可以用的工具边界：Node（仅跑 build.js）、Cloudflare Pages Functions（原生 JS 文件）。

---

## 5. 项目结构

```
whizzzest.com/  （E:\Projects\website-group\whizzzest\whizzzest.com）
├─ src/                        # 源码
│  ├─ partials/                # 公共片段（构建时拼入每页）
│  │  ├─ head.html             # <head> 骨架，含 {{title}} {{description}} 占位符
│  │  ├─ header.html           # 顶部导航（logo + 6 个栏目 + 语言切换）
│  │  └─ footer.html           # 页脚（友情链接/联系/公众号二维码 ×3）
│  ├─ pages/                   # 中文页面，每页完整正文
│  │  ├─ index.html            # 首页
│  │  ├─ tourism.html          # 旅游线路
│  │  ├─ heritage.html         # 非遗文化
│  │  ├─ cuisine.html          # 美食特产
│  │  ├─ fireworks.html        # 烟花产业 + 数字烟花
│  │  └─ about.html            # 关于/联系
│  ├─ en/                      # 英文版（结构与 pages/ 对应）
│  ├─ assets/
│  │  ├─ css/style.css
│  │  ├─ js/main.js  fireworks.js  player.js  contact.js
│  │  └─ img/                  # 旧站下载的 10+ 张图（WebP 已优化）
│  └─ data/                    # 可选：JSON 内容文件（JS 读取渲染列表类内容）
├─ functions/
│  └─ api/
│     ├─ contact.js            # 表单：校验 → D1 → 企业邮通知
│     └─ visit.js              # 计数（KV，可选）
├─ public/                     # 原样复制到 dist 根
│  ├─ favicon.svg
│  ├─ robots.txt
│  ├─ sitemap.xml
│  └─ audio/主题曲.mp3         # 从旧站提取下载
├─ build.js                    # 零依赖构建脚本（Node 内置模块）
└─ dist/                       # 构建产物（gitignore，部署到 Pages）
```

---

## 6. 页面与内容规划

| 新页面 | 内容来源 | 说明 |
|---|---|---|
| index.html 首页 | 旧站全部板块整合 | Hero + 核心特色 + 非遗 + 美食 + 烟花产业 + 主题曲 + 关于 |
| tourism.html | 旧站"查看旅游线路" | 万载古城/竹山洞/九龙原始森林等，卡片/时间线排版（非地图） |
| heritage.html | 旧站非遗板块扩写 | 花炮/得胜鼓/开口傩/夏布 + 图集 |
| cuisine.html | 旧站美食板块扩写 | 六大碗等 + 图集 |
| fireworks.html | 旧站烟花产业 + 数字烟花 | 产业介绍 + Canvas 烟花交互页 |
| about.html | 旧站关于/友情链接/联系 | 公众号二维码 ×3、GitHub/Gitee/抖音、邮箱 |
| en/* | 旧站 /en | 英文版对应页面 |

**交互模块（原生实现）**：
- 数字烟花：`fireworks.js` 用 Canvas + requestAnimationFrame 画粒子烟花（约 300 行），支持类型/特效/音效/全屏
- 主题曲：原生 `<audio>` + 自定义 UI（播放/暂停/进度条）
- 导航响应式：CSS media query + JS 汉堡菜单
- 懒加载：`loading="lazy"` + `decoding="async"`

---

## 7. 边缘函数（Pages Functions）

| 函数 | 方法 | 职责 | 依赖 |
|---|---|---|---|
| /api/contact | POST | 表单校验（Honeypot 防垃圾）→ 写入 D1 → SMTP 发企业邮通知 | D1 + SMTP（Secrets 存凭据） |
| /api/visit | POST | KV 原子计数（节流防刷），可选展示 | KV |

**邮件说明**：Workers 出站支持任意 TCP 端口（`connect()`），可用 smtp-client 直连 `smtp.qiye.aliyun.com:465`；若受限，降级为 Cloudflare Email Routing 转发或表单结果存 D1 人工查收。**企业邮 DNS 记录一律不动**。

---

## 8. 域名与 DNS 方案（**已定：whizzzest.com 主域**）

### 8.1 决策记录（v2.1 拍板）

- **新站域名 = `whizzzest.com`（主域 @ 与 www 均服务网站）**，不再拘泥旧 .top 域名
- whizzzest.com 已在 Cloudflare 账号内（Zone active），无需迁移 NS
- 9 条阿里云企业邮记录（MX/SPF/DMARC/CNAME）**全部保留不动**——网页与邮箱在同一 Zone 共存，互不影响

### 8.2 落地方式

| 记录 | 类型 | 指向 | 说明 |
|---|---|---|---|
| `@` | CNAME（Cloudflare 自动 CNAME Flattening） | Pages 项目 | 根域名服务网站 |
| `www` | CNAME | Pages 项目 | 统一到 www 或 301 到 @ |
| 企业邮 9 条 | 保持现状 | 阿里云企业邮 | **严禁改动** |

- Pages 自定义域名添加 `whizzzest.com` + `www.whizzzest.com`，SSL 自动签发（Full strict）
- 旧域名 `whizzzest-yanjingwanzai.top`：迁移完成验证前**不动作**；新站上线稳定后，由用户决定保留（301 到新站）还是随 ECS 一起下线（省服务器费用）

---

## 9. 部署流水线

```
src/ ──▶ build.js（拼接公共片段 + 替换 meta 占位符）──▶ dist/
                                                          │
GitHub push main ──▶ Cloudflare Pages（Git 集成）         │
   构建命令：node build.js   输出目录：dist/ ◀────────────┘
   （也可：wrangler pages deploy dist --project-name=...）
```

- **无 npm install 步骤**——build.js 零依赖，构建即文件拼装复制
- Secrets：SMTP 账号密码、D1/KV binding 在 Pages 项目设置
- 预览：非 main 分支自动生成 `*.pages.dev` 预览链接

---

## 10. 实施步骤（分阶段）

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P0 决策** | ✅ 已定：原生三件套、域名=whizzzest.com 主域、仓库=whizzzest.com；待定：表单 | 本方案定稿 |
| **P1 骨架** | 仓库目录 + build.js + partials（head/header/footer）+ _headers/robots/sitemap 生成 | 可构建空站 |
| **P2 内容迁移** | 下载旧站全部图片/音频，整理文案，重排信息架构（按访客动线） | 素材库 + 文案表 |
| **P3 页面开发** | 6 中文页 + 英文版（独立撰写）+ 数字烟花/音频/无障碍 | 全站页面 |
| **P4 边缘函数** | contact（如选）+ visit + D1/KV 绑定 | API 就绪 |
| **P5 部署上云** | Pages 项目创建、绑定 whizzzest.com @+www、SSL、_headers 安全头 | 线上可用 |
| **P6 验证** | SEO/sitemap/JSON-LD、移动端、性能预算、Web Analytics、邮件发信测试 | 验收通过 |
| **P7 旧站处理** | 确认无误后由用户决定 .top 301 保留或随 ECS 下线（省钱） | 迁移完成 |

---

## 11. 风险与注意事项

1. **导航/页脚维护**：集中在 partials/ 改一处，build.js 全站生效（这是原生方案的关键设计，务必遵守"公共部分只改 partials"）
2. **双语同步**：en/ 内容改动需手动同步中文版——宣传站内容低频更新，可接受
3. **无 TypeScript**：用 JSDoc 注释（`/** @type {number} */`）+ VS Code 可获类型提示，弥补部分类型安全
4. **免费额度**：Pages 静态无限；Functions 10 万请求/天；D1 5GB——宣传站量级完全够
5. **SMTP 发信**：若 465 端口直连受限，走 §7 降级方案
6. **图片版权**：旧站图片注意来源标注，逐步替换自有拍摄图
7. **主题曲音频**：从旧站 JS 定位音频真实 URL 下载到 public/audio/，勿引用旧站外链
8. **框架迁移后悔药**：原生结构清晰，未来若内容膨胀到 >10 页，迁移到框架成本很低（页面和内容都是现成的）
9. **二维码**：公众号二维码是实体物料，方案 A 下 URL 不变，零影响

---

## 12. 决策记录

- [x] **决策 1**：域名 → **whizzzest.com 主域 @+www**（v2.1 已定；企业邮记录不动，旧 .top 后续单独处理）
- [x] **决策 2**：技术路线 → **原生三件套**（已定）
- [ ] **决策 3**：联系表单是否需要（需要则提供企业邮 SMTP 授权码，或走 Email Routing；不需要则先用 mailto 直链）
- [x] **决策 4**：仓库 → **whizzzest.com**（已连接，空仓库即代码根）

P1 骨架可随时开工。
