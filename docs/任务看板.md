# 任务看板 —— 多会话并行协作声明台

> 铁律见仓库根 [CLAUDE.md](../CLAUDE.md)。本看板是**唯一的**任务声明、独占资源登记与跨会话交接处；多会话同一工作区并行，靠这张表互相可见，不靠猜。

## 使用规则

1. **开工先登记**：在「进行中」表加一行（任务、会话标识、文件范围、独占资源）；结束把状态改 ✅ 并在「已完成」留档交付物（部署 id / commit）；
2. **独占资源**：`wrangler deploy`（游戏站/主站）与 `upload-games.mjs`（R2 上传器）运行期间独占——其他会话不得并发部署、不得改 `games.json` / `games.en.json`；登记撤销即释放；
3. **跨会话交接挂「待认领」**：写清事项与验收标准，认领者把会话标识写进表格——跨流水线的工序不许口头交代（EN 元数据欠账的教训）；
4. 冲突事故修完后在页末「冲突事故复盘」补记根因与改进。

## 进行中

| 起 | 任务 | 会话 | 文件范围 | 独占资源 | 状态 |
|---|---|---|---|---|---|
| 2026-09-10 | 批次二a 滚动收录上线（首批 12 款已上线，上传器推进剩余款，逐款「自愈→登记」滚动） | 批次二a 会话 | games.json、games.en.json、game.css、game-shell.js、upload-games.mjs | R2 上传器 + 游戏站部署权 | ✅ 首批 12 款上线（3b27e156）+ push 事故 filter-branch 修复（链尾 23d8335）；剩余 4399 款滚动移交「批次二c」 |
| 2026-09-10 | 街机死神 4399-196556 定分辨率适配：entry 注入垫片（fit 缩放+输入重映射+焦点回笼三件套）+ 方案落档 §7.8 | 适配垫片会话 | game/r2-assets/g/4399-196556/**/index_https.html、games 素材库镜像同路径、docs/游戏整合方案.md（§7.8 新增）、docs/上线记录.md、本看板 | R2 定向重传 3 对象次（批次二a 滚动上传间隙完成，键集不相交无冲突） | ✅ 线上已生效：几何偏差 0px + 点击进关 + 全屏键盘存活 |
| 2026-09-10 | 批次五+二b+四：CatCity（存档镜像补丁）/EmberQuest（清洗）/life-restart/proxx 构建 + DOS 专区（js-dos 内核+12 款，games.json 已登记 16 条 EN 同步） | 批次五二b四会话 | game/r2-assets/g/{cat-city,emberquest,life-restart,proxx,dos}/、games.json、games.en.json、docs/游戏整合方案.md（批次记录）、docs/上线记录.md、本看板 | R2 上传器 + 游戏站部署权（**排队等批次二a 释放**，本地验收已过半） | ✅ **已上线**：批次二a 部署链尾 71e7dc22 携带 games.json +16 条上线（本会话免再部署，live↔disk 一致）；R2 793 对象全就绪；线上 9 路由 curl 全 200（MIME 正确、19.3MB 包正常）；交付 commit f59ca8a（push 连坐 91ef04f blob 事故，随全线解堵后发布）；上线记录已补记 |
| 2026-09-10 | 坏游戏修复①（业主拍板）：成语亨特H5 更名「习语猎手H5」+ 双语文案纠偏（实为英文 Idiom Hunt，zh/EN 卡「汉字成语/Chinese interface」失实）；修复雷电征服世界 4399-209828、火柴人激斗英雄 4399-224419 | 诊断修复会话 | games.json、games.en.json（仅 226229 条目增量）、game/r2-assets/g/{4399-209828,4399-224419}/ 新增文件、games 素材库镜像同路径、本看板、docs/上线记录.md | R2 定向传 ~70 对象（键集不相交，批次二a 间隙执行） | 🔄 R2 修复部分线上已生效并真浏览器验收 ✅（雷电：缺 fba.js.mem.dat→ROM roms/cps/1944.zip.gz.dat 15.8MB，FBA 模拟器 boot+操作说明弹窗响应输入；火柴人激斗：缺 assets/main/* → Cocos 主包+internal/resources bundles+native 贴图共 ~65 文件逐层懒加载补齐，标题画面渲染进角色创建，残留 404 个位数；上传走 `wrangler r2 object put --pipe`（stdin），本会话 `--file` 路径 fetch failed 可复现、`--pipe` 稳定，供撞同坑的会话参考）；**文案增量已随 71e7dc22 上线**（线上 games.json「习语猎手H5/英语词谜」+ EN「In-game text is English」核实 ✅，games*.json 已随 91ef04f 入库）；docs 两份（37ed4ae）push 被事故 blob 连坐拒绝，待批次二a 修 91ef04f 后随队发布（见冲突事故复盘） |
| 2026-09-10 | 外链专区扩展（决策⑰，业主拍板）：7 家海外云平台（GFN/xCloud/PS+ 云串流/Boosteroid/Shadow/Luna/Blacknut）并入「云端畅玩」platforms + 12 家商店/分发平台（Steam 等）新开「正版平台」紧凑链接分区（games.json 顶层 stores 字段）；方案已入档 docs/游戏整合方案.md §8.6 | 外链扩展会话 | games.json、games.en.json、game/public/index.html、game/public/en/index.html、game/public/assets/css/game.css、game/strings/strings.zh.json、game/strings/strings.en.json、docs/游戏整合方案.md（§8.6 已入档）、本看板 | 游戏站部署 1 次（**等批次二a 释放窗口**；games*.json 编辑同窗口，实施前核对 live↔disk） | ✅ 线上已生效：批次二a 部署链尾 71e7dc22 携带上线，免再部署——live↔disk games.json 字节级一致 + 生产 puppeteer zh/EN 17 断言全过（上线记录已补记） |
| 2026-09-10 | 下架 飞机大战H5 4399-209197 + 疯狂猜歌 4399-218084（业主拍板「完全彻底」：h5mini 云空壳 + 源站已下架，无修复价值）：games.json/games.en.json 删条目 + R2 删对象 + r2-assets 与素材库镜像删目录 + 游戏站部署 1 次生效 | 诊断修复会话 | games.json、games.en.json（删 2 条）、game/r2-assets/g/{4399-209197,4399-218084}/、games 素材库镜像同路径、本看板、docs/上线记录.md | R2 删 2 对象 + 游戏站部署 1 次（批次二a 间隙执行） | ✅ 线上已生效：部署 03007857；games.json 43 条零残留、/g/ 直链 404、/play/<id>/ 落「可能已下架」提示、抽查零回归 |
| 2026-09-10 | 下架第二批：习语猎手H5 4399-226229 + 雷电征服世界 4399-209828 + 泡泡坦克3H5 4399-225752（业主看图圈定「彻底删除」；前两款系本会话刚修复/纠偏过，业主体验后仍决定砍）：同上全套流程（games.json/games.en.json 删 3 条 43→40 + R2 删 36 对象 + 目录清干净 + 部署） | 诊断修复会话 | games.json、games.en.json（删 3 条）、game/r2-assets/g/{4399-226229,4399-209828,4399-225752}/、games 素材库镜像同路径、本看板、docs/上线记录.md | R2 删 36 对象 + 游戏站部署 1 次（批次二c 间隙执行） | ✅ 线上已生效：部署 b2177ed1、commit 034d537 已 push；games.json 40 条零残留、/g/ 直链全 404、抽查零回归 |
| 2026-09-10 | Proxx 太空扫雷线上不可玩热修：Worker 文件 index-ee4eb166.js 缺 AMD 垫片（rollup loadz0r `prependLoader` 产物在收录批次五重建时丢失）→ Worker 内 `define is not defined` 秒死、stateService 永不就绪、点开始永久「…Loading…」；修复=文件头部前置与 index.html 相同的 `if(!self.define){…}` 垫片（worker 分支走 importScripts），无需重新构建 | Proxx 热修会话 | game/r2-assets/g/proxx/index-ee4eb166.js、本看板、docs/上线记录.md | R2 定向重传 1 对象（键集不相交，--pipe 路径） | ✅ 已上线并验收：线上 sha256 与磁盘一致（2482cfffa017e061）；真浏览器直连 /g/proxx/ + 嵌入 /play/proxx/ 双路径点 Start 网格渲染、零 define 报错（截图 .build-tmp/_probe_proxx_live-*.png）；交付=本行 + 上线记录 |
| 2026-09-10 | 魔塔50层3D 4399-212029「菜单点不动」诊断定论 + 体验补丁（业主拍板）：诊断=游戏本身完好，原版菜单仅键盘操作（真实鼠标事件点按钮无反应系原包行为，Enter 直接开局；savedata0.es3 控制台报错为首跑无档固有日志，无害）；补丁=zh/EN controls 补「方向键选择+Enter 确认、菜单不支持鼠标」+ 壳层对 keyboardOnly 游戏通用提示（进页 1 次 + 首次点击游戏区 1 次 toast，24 款纯键盘游戏全受益） | 魔塔诊断会话 | game/public/games.json、games.en.json（仅 212029 controls 增量）、game/public/assets/js/game-shell.js、game/strings/strings.en.json、game/public 构建产物（build-game.mjs 重建 play 页）、本看板、docs/上线记录.md | 游戏站部署 1 次（各会话均无在办部署登记；部署前重读本表核对） | ✅ 线上已生效：部署 f904d45e（6 文件恰好预期增量）；check-i18n 全绿；本地 zh/EN 双提示 + Enter 开局真浏览器验收 ✅；线上 controls 双语 + zh/EN toast 核实 ✅；**同日业主拍板收窄**：提示不再挂 keyboardOnly 全量生效，改按款标记 kbHint 仅魔塔启用（games.json 新机制字段 + check-i18n MECH 名单同步；其余 23 款纯键盘游戏业主认为有鼠标/触屏可用不弹）——重部署 eacdab6b（恰 2 文件），40 款线上全量复扫 0 误 0 漏 |
| 2026-09-10 | DOS 专区 12 款线上全黑「不可玩」热修（业主报告+截图）：收录时漏带官方 js-dos.css——js-dos v8.4.1 为 js+css 双文件分发，js-dos.js 不含任何样式（已核 sha256 与 npm@8.4.1 一致且无 CSS 内嵌）→ UI 全靠 tailwind/daisyUI 类无样式可依：预运行窗口透明、播放键黑色 SVG 贴黑底，玩家见纯黑屏且无任何可点物；此前「本地双款引导成功」系自动化验收合成点击了不可见按钮，真机从不可点。修复=补 js-dos.css + 12 个 dos-*/index.html 加 <link> + emulators/ 补 file-explorer.js/css（js-dos.js 懒加载引用防 Upload 对话框 404） | DOS 热修会话 | game/r2-assets/g/dos/js-dos.css（新增）、game/r2-assets/g/dos/emulators/file-explorer.{js,css}（新增）、game/r2-assets/g/dos/dos-*/index.html（12 个加 link）、本看板、docs/上线记录.md | R2 定向传 15 对象（键集不相交，--pipe 路径；不碰 games*.json、不占 Worker 部署） | ✅ 已上线并验收：15 对象 --pipe 传毕即时生效（线上 200/MIME 正确、css 与 file-explorer sha256 磁盘一致、12 页 link 全在）；真浏览器验收=本地 12/12 + 线上 direct/shell 3 款 6/6（窗口不透明 rgb(29,35,42)、292×292 播放键命中、canvas 引导），截图 .build-tmp/dos-fix-*.png / dos-livefix2-*.png；check-i18n 全绿；上线记录已补记；**验收断言已升级为「可见性+真鼠标点击」，防合成点击误判再犯**；**同日二次热修**（业主报告「点进去载入的都是同一个游戏」）：12 页共用相对 `url:"bundle.jsdos"` 致 js-dos 以 URL 串为存储键全部碰撞——第一款玩过的游戏快照被其余 11 款直接载入；改 12 页 url=各自绝对路径（键唯一，污染键成孤儿），R2 重传 12 html，线上连续玩序验收各自 bundle 请求 1 次/页、跨游戏 0 ✅（详见上线记录二次热修行） |
| 2026-09-10 | 魔塔50层3D 4399-212029 画面放大 fit 垫片（业主拍板）：原包 gameIndex.html 写死 960×540 且 UnityLoader 无 resize 逻辑 → 大屏四周大黑边、全屏不放大；垫片=entry 注入等比缩放（fixed 居中 + scale(min(vw/960,vh/540)) + resize 跟随；桌面 ~1.6×、手机从溢出变全显、全屏真正铺满；transform 缩放由浏览器换算鼠标命中，Unity 内部分辨率不变）；对齐街机死神适配 §7.8 先例 | 魔塔诊断会话 | game/r2-assets/g/4399-212029/…/gameIndex.html、games 素材库镜像 crawled/games/212029 同路径、本看板、docs/上线记录.md | R2 定向重传 1 对象（键集与 DOS 热修 15 对象不相交，--pipe 路径） | ✅ 线上已生效：R2 定向重传 gameIndex.html 1 对象（--pipe，即时生效零部署）；本地/线上双验收：1280×800 与 1000×700 两视口 canvas 实铺尺寸精确=期望值、缩窗实时跟随、生产 /play/ 页 canvas 1280×720 精确铺满；输入无回归定论=三态判别实验证明该游戏无任何鼠标处理（缩放/无缩放点「菜单[Esc]」均无响应系原包纯键盘行为），键盘（Enter 开局/Esc 菜单）缩放下正常 |
| 2026-09-10 | 全项目扩展性/精简诊断落地（业主拍板）：**A 档执行**——A2 归档一次性脚本 ×14（game/scripts 8 + 顶层 6，移入各自 archive/ 子目录，**只移动不删除**）；A4 build.js 瘦身（SOCIAL_ICONS 11.5KB SVG 移出构建脚本 + sitemap/robots 动态路由读统一注册数据，dist 产物字节级对照验收）；A3 main.js 按页拆分（music-player.js/home-hero.js 拆出、仅对应页面加载，全站每页少背 ~450 行）；A5 check-i18n 增 games.json schema 校验（11 必填 + 字段白名单 + 类型，拦截级，先验证存量 40 款全过再合入）；A1 已由瘦身会话完成（本表瘦身行）不重做。**B/C 档落档 docs/结构与精简方案.md**（B1-B7 结构性改进立项候选 + C 档明确不做清单） | 结构精简会话 | docs/结构与精简方案.md（新建）、本看板、game/scripts/archive/**、scripts/archive/**（均为归档移动）、build.js、src/assets/js/{main.js,music-player.js,home-hero.js}、src/pages/index/index.html、src/pages/music/index.html、game/scripts/check-i18n.mjs、admin/index.js（仅 AI 空态提示一行 ai-seed.sql 路径随 A2 同步）、scripts/smoke.sh（仅指纹项数注释随 A3 同步）、worker/music.js（/music/ 系 Worker 动态页，无 src/pages/music）| 无部署独占；push 即 CI 自动部署 4 Worker（每次 push 前跑 node build.js + git status 三查） | ✅ **A 档全部完成**：A2=21a0fc9（归档 ×14+README）、A4=6f0dce0（SOCIAL_ICONS 入 JSON+DYNAMIC_SECTIONS 注册表，dist 字节级一致）、A3=220e796（main.js 771→383 行拆出 home-hero/music-player，线上 / /en/ /music/ /about/ 加载面与 200 核验 ✅，CI 部署 34462280532）、A5=f04c39a（schema 校验拦截级，40 款全绿+6 坏用例全抓）；A1=瘦身会话 1df9034 不重做；B/C 档落 docs/结构与精简方案.md（§五 B1-B7 立项候选待逐项拍板） |
| 2026-09-10 | 火柴人激斗英雄 4399-224419 残留 404 清零（接「坏游戏修复①」行）：业主报控制台 404 轰屏；深探针实跑揭示远超「个位数」——之前缺的 native 贴图传上后游戏推进更深，又触发 ~190 个懒加载资源未传 R2（批次二a 只补到角色创建层）；另 getUniqueDevid 是早期回源误存的 4399 Apache 404 HTML（非 JSON，JSON.parse 抛栈），已改合法 `{"code":200,"data":…}` 桩；字体 演示夏行楷（数字）-new.ttf 源站已删（回源真 404），用包内同系 jxhc2.ttf 替位（FontFace 按资产路径加载，不看文件内部族名）；教训：R2 键=去 `/g/` 前缀，勿带 `g/`（首次传错已删） | 火柴人清零会话 | game/r2-assets/g/4399-224419/**（新增+3 处修复）、games 素材库镜像同路径、本看板 | R2 定向传 ~200 对象（键集不相交，--pipe 路径；与 DOS 热修/魔塔垫片对象不相交） | 🔄 执行中（heal 闭环：探测→回源→传 R2 循环至零 404） |
| 2026-09-10 | 仓库瘦身（业主拍板方案 A）：`game/r2-assets/` 1.0G/5479 文件从 git 历史**全量剥离**（git filter-repo --invert-paths）+ untrack + gitignore 总则；`reference/` 246M（旧站克隆+firework 源码）删除。根因=游戏资产镜像入库致 .git pack 677M、push 100MB 硬限事故（本表待认领 09-10 行）；预期 .git 725M→约 30M、磁盘 1.5G→约 0.9G。R2 是游戏资产交付端，线上零影响、零部署 | 瘦身会话 | .git（历史改写，全 commit hash 变更）、.gitignore、本看板 | **git 历史改写 + force push 权**：执行窗口内其他会话暂缓 commit/push；完成后无需重 clone（共用工作区），引用旧 commit id 失配属预期（看板/上线记录内旧 id 为改写前留档） | ✅ 完成：filter-repo --invert-paths 剥离，200 commit 全保留（hash 全变更）；**.git 725M→39M（pack 39.12MiB）、跟踪内容 1061.5MB→14.6MB**；r2-assets 磁盘 1.0G 完好供上传器、reference/ 246M 已删；force push 落地（f3c96f8→1df9034 forced update）本地远端追平、工作树 clean；零部署、线上零影响；后续 r2-assets 新收录增量**只传 R2 不入 git** |
| 2026-09-10 | 未上架款清退（业主拍板「直接删除，彻底一点，看起来不好玩」）：`4399-250482`=**数独进阶者**（3.7M/56 文件，自愈期传过 R2 键）+ `4399-256363`=**植物大战僵尸3D版**（仅 8K Cocos 骨架残缺无入口，R2 无键 404 已证）——本地目录删除 + 250482 R2 键逐个删除（wrangler r2 object delete，不动部署权）；两款均未登记 games.json，零部署零 i18n | 清退会话 | game/r2-assets/g/{4399-250482,4399-256363}/、R2 前缀 4399-250482/*、本看板 | R2 删键操作（键集与在办会话不相交） | ✅ 完成：本地 2 目录删除 ✅；R2 侧因目录先删无键清单，改用**真浏览器实跑收集 200 响应键（48）+ 静态引用闭包 HEAD 补充（5）+ 壳页 index.html 补删（1）=54 键全数删除**，抽验+入口复查全 404（教训：删本地目录前应先落盘键清单）；256363 R2 无键零动作；games.json 未动、零部署；探针与键清单留档 .build-tmp/250482/ |
| 2026-09-13 | 主站 footer 友情链接新增 Nmail（业主新项目，https://nmail.whizzzest.com ，中英双语都要）：src/data/site.json + src/data/en/site.json friendLinks 各追加 1 条；改后 node build.js 重建 + check-i18n；push 走 CI 自动部署 | footer 友链会话 | src/data/site.json、src/data/en/site.json、本看板 | 无（push 触发 CI 自动部署 4 Worker） | ✅ 完成：commit 4e1699d；build.js 过 + check-i18n 全绿 + dist 中英 footer partial 各含 Nmail ✅；CI 部署 34745349047（1m13s）；线上核验 zh/EN footer partial + zh/EN 首页 nmail 链接全命中 ✅ |
| 2026-09-13 | IM 板块 M1 上线（业主拍板做 E2EE，方案 docs/IM聊天方案.md v1.1）：新 Worker whizzzest-im（im/ 目录）+ DO IMRoom/RateLimiter + D1 006 迁移 8 张 im_ 表 + 双方式注册/登录（手机号+密码｜邮箱+验证码，im_email_codes 独立表防三门户互覆）+ **E2EE 密钥目录**（本机 ECDH P-256 + 密码包裹私钥备份可换机恢复）+ /app SPA 骨架；CI 增 im dry-run+deploy。**M2（好友+1v1 E2EE 实时通道）待续** | IM 会话 | im/**（wrangler.jsonc/index.js/public/）、scripts/migrations/006-20260913-im-tables.sql、.github/workflows/deploy.yml、docs/{IM聊天方案,上线记录,密钥清单}.md、本看板；im/reference/ 仅磁盘不入 git | D1 远程迁移（已执行）+ whizzzest-im 首次部署（已执行，本地 wrangler 86aea8ae-1917-42c9-a71f-0d9810308088 + 3 secrets 新配；commit 31f55b4 push 触发 CI 自动部署 5 Worker，run 34763086220 ✅ 含 im 首次 CI 部署，线上复验 / 200） | ✅ M1 完成并上线：真浏览器本地+线上双验收——注册→本机生成密钥→登出→重登密码恢复同指纹（本地 C0 48 5D 27…/线上 1E 13 47 FE…）✅；线上 / 200、/api/me 401、坏密钥 400 'keys'、/app 未登录 302、资产 200 ✅；邮箱验证码注册/登录+错码拒绝 ✅；资料改存入 D1（E.164/公钥 65B/包裹备份）✅；验收测试账号已删，线上 im_users 清空 |
| 2026-09-13 | IM 登录页 UI 对齐三门户（业主报修 3 项：页脚缺「焰境密语」标签链接、手机号登录没抄好、logo 破图）：① shared/portal-ui.js 页脚补「焰境密语」链接（商户/作者/IM 三门户统一页脚，随 CI 部署全量生效）② im 面板照抄 loginPanel 组件——区号选择器改 acc 覆盖层+adiv 分隔+phoneCountryOptionsHtml 全量国家（顺修 country='86' 非 ISO 致海外号误判 CN 的隐藏 bug）、验证码行改 acode+abtn、提交按钮 abtn→aprimary、发码冷却仅成功后启动 ③ logo 照抄 writer 同款 data URI（im 自嵌 base64 缺 `<svg>` 开标签致破图）④ 顶栏去重复「前往官网」（对齐 writer 只留返回官网↗） | IM UI 修复会话 | shared/portal-ui.js、im/index.js、本看板、docs/上线记录.md | 无（push 触发 CI 自动部署 5 Worker） | ✅ 完成：commit b17cd40；writer/merchant/im 三 Worker dry-run + check-i18n 全绿；CI 部署 34765184307（5 Worker 全量）；线上核验 ✅——im 登录页/注册页、writer /login、merchant 登录页页脚「焰境密语」链接全在；im logo 与 writer 字节级一致且解码含 `<svg>` 开标签；/register 新路由 200、旧结构（im-subtabs/im-cc/im-code-btn/前往官网）零残留；本地真浏览器登录/注册/邮箱注册三视图截图通过 |
| 2026-09-13 | IM M2（好友+1v1 E2EE 实时通道，方案 §5/§6/§9）+ **im/index.js 模块化拆分**（业主拍板「一个文件不要什么功能都有，文件分块」）：拆分＝index.js 薄入口（路由分发+安全头+DO 导出）+ src/{config,util,session,mail,ws}.js + src/api/{auth,social,convs}.js + src/do/{room,rate-limiter}.js + src/pages/auth.js；前端 app/api/ui/profile/convlist/contacts/chat/ws + im-crypto 增信封与消息加解密。M2 内容＝好友搜索/申请/同意/拒绝/取消/删除/拉黑 + dm 幂等建会话（信封随建会话提交）+ DO IMRoom Hibernation 实时收发/typing/在线 + 历史分页解密 + 未读已读 | IM M2 会话 | im/**（index.js、src/**、public/assets/**）、docs/IM聊天方案.md（v1.2 补记）、docs/上线记录.md、本看板 | 无独占（push 走 CI 自动部署；本地 wrangler dev 从 im/ 目录单实例自管） | ✅ 完成：ec251e0（拆分，M1 全回归过，CI 34767153852）+ 6119bb3（M2）+ a0c0a24（WS 冷启动 outbox 修复）；CI 34768834463+34769238213 success；本地真浏览器双账号全链路（互发实时解密/离线补拉/密码恢复解历史/拉黑拒收/D1 密文核验）+ 线上双账号互发复验全过；线上测试账号已精确清除；详见上线记录 |
| 2026-09-13 | 主站导航「焰境万象」组加「焰境密语」入口（业主提前 IM 方案 §9 拍板项「导航入口随 M4」；v1 仅中文，en 导航不动）：src/data/site.json 焰境万象 menu 追加 1 条 → https://im.whizzzest.com ；node build.js 重建 + check-i18n；push 走 CI 自动部署 | 导航入口会话 | src/data/site.json、本看板、docs/上线记录.md | 无（push 触发 CI 自动部署 5 Worker） | ✅ 完成：commit ceb0333；build.js 重建 dist（zh header 桌面面板+移动抽屉两处均含新项）+ check-i18n 全绿 + en partial 核验 0 波及；CI 部署 success；线上 zh 首页导航含「焰境密语」✅、/en/ 0 残留 ✅ |
| 2026-09-14 | IM M3 群组 E2EE（方案 §9 M3：建群（直接拉人制）/拉人/退群/踢人/解散/改名 + **重钥** + system 消息 + 群未读）：im/src/api/convs.js 增群组 API（建群/拉人/踢人/退群/解散/改名/成员列表）、im/src/do/room.js 增成员变更帧（kicked 等）、system 消息落库（明文不加密）；前端 im-crypto 增多成员信封生成/新 K 解包、contacts 增建群流、chat 增成员管理 UI；验收=3 浏览器群聊扇出有序、踢人重钥后被踢者解不开新消息、100 人群信封正确 | IM M3 会话 | im/**（index.js、src/**、public/assets/**）、docs/IM聊天方案.md（v1.3 补记）、docs/上线记录.md、本看板 | 无独占（push 走 CI 自动部署；本地 wrangler dev 从 im/ 目录单实例自管） | ✅ 完成：commit 136f1a5（16 文件 +1028/-141，新模块 members.js）+ 5cd3946（docs）；CI run 34793507979 success；本地 3 客户端脚本 42 断言 + 100 人群压测 10 断言 + 真浏览器双账号 UI 全链路全过；线上核验（/、/register、新资产、注册 uid=5 冒烟）✅ 后测试账号已精确清除；顺修 M2 全局限流失效 bug（DO ID 当 stub + GET 未计数）；seq 分配改标量子查询（v1.3）；详见上线记录 |
| 2026-09-14 | IM M4 加固+上线收尾（方案 §9 M4，导航入口已由导航会话 ceb0333 提前完成故不在本行）：① 举报闭环——POST /api/reports（成员校验+理由≤200+限流）+ 新模块 im/src/api/reports.js + 前端 dm/群菜单「举报」入口 + 落库可查核验；② 新号限流——注册<1h 账号 5 条/min（DO 内按 uid 账号年龄缓存）；③ 限流加固顺修——IMRoom 全局 RateLimiter 原整会话共用首个发送者 IP 建键（他人消耗其预算），改按发送者 IP 分键；④ 安全数字——1v1 聊天窗头部改双方公钥联合 SHA-256 安全数字（原单侧指纹），群成员逐员指纹保留；⑤ 顺修 chat.js dm 菜单 `blocked` 未声明（ESM 严格模式 ReferenceError，拉黑/删除按钮自 M2 起渲染不出，M2 验收走 contacts 视图未暴露）；⑥ 重连/多标签脚本验收 + 文档 v1.4/上线记录 | IM M4 会话 | im/index.js、im/src/config.js、im/src/api/reports.js（新增）、im/src/do/room.js、im/public/assets/js/{ui,chat}.js、docs/IM聊天方案.md（v1.4 补记）、docs/上线记录.md、本看板 | 无独占（push 走 CI 自动部署；本地 wrangler dev 从 im/ 目录单实例自管） | ✅ 完成：commit b7d1f6b（9 文件 +134/-22，新模块 reports.js）+ CI run 34795924352 success；本地脚本 18 断言全过（举报校验矩阵+D1 落库逐字段/新号第 6 条 err rate+老号对照/同 uid 双 WS 多标签同 seq 广播/搜索 429/安全数字对称性）+ 真浏览器核验（安全数字 6 组展示、dm/群菜单含举报、举报 prompt→POST→D1 全链路、系统消息正常）；线上核验（im / 200、ui.js 含 safetyNumber、/api/reports 未登录 401、主站 zh 导航入口两处在位）；esbuild 双入口 + dry-run + check-i18n 全绿；**IM 板块 M1-M4 至此全量完结** |
| 2026-09-14 | IM UI v2 业主真机反馈热修 6 项（本轮截图报修）：① 添加好友搜索结果去掉公钥指纹外显+附言改醒目 textarea；② /register 已登录态不再 302 /app（原 `if (user) redirect('/app')` 对 / 与 /register 一刀切，点「注册焰境密语」像自动登录）；③ 验证码邮件主题假码后缀（`code.slice(0,2)+时间戳base36`，标题 76xf3d ≠ 实际 764501）——im 与 writer 同款修掉（merchant 本就干净）；④ 好友申请页去 UID 兜底文案+同意/拒绝/撤回后就地重绘不再整页等网络（「收到的申请」标题闪没）；⑤ 设置页加密方式只说「端到端加密」+去公钥指纹行；⑥ 气泡限宽 max-width:72% 误加在 .im-msgrow 行上（己方消息只靠到 72% 处贴不到右边）——移到 .im-bwrap/.im-msgrow.mine .im-bubble | IM 反馈热修会话 | im/index.js、im/src/mail.js、writer/index.js、im/public/assets/js/{contacts,profile}.js、im/public/assets/css/im.css、本看板、docs/上线记录.md | 无独占（push 走 CI 自动部署；本地 wrangler dev 从 im/ 目录单实例自管） | ✅ 完成：esbuild transform+worker bundle+wrangler dry-run（im/writer）+check-i18n 全绿；真浏览器本地双账号全链路（搜甲无指纹+附言 textarea/申请页无 UID/同意 22ms 就地重绘标题恒在不重拉/dm 气泡几何量化贴边 20px 内边距/设置页只说端到端无指纹/已登录 /register 出表单而 / 仍 302）+console 零报错；merchant 主题经查本就干净未动；commit 28c1ee0 CI run 34816235562 success；线上核验（im / 与 /register 200、contacts.js/profile.js 零指纹引用、im.css 气泡新规则生效）✅；详见上线记录 |
| 2026-09-14 | IM UI 改版 v2（业主拍板 4 项，方案 docs/IM-UI改版方案.md）：WhatsApp Web 风格三栏（icon 栏+列表面板+聊天主区）+ 暗色专用全 CSS 变量（预留亮色）+ 资料与设置双视图（头像=个人资料含邮箱绑定/⚙=设置）+ 联系人「新聊天」面板式 + 信息抽屉 + 未读分割线 + **1v1 已读回执**（convs 带 peer_last_read + /read 推进时 DO 广播 read 帧）；**业主真机预览 10 项反馈当日全部修订**（§7：菜单外点关闭/Enter 发送/消息贴边/安全数字不外显/邮箱绑定 POST /api/me-email+email-code purpose=bind/UID 全站隐藏/搜索框简化校验/抽屉 X 常显/设置资料拆分/图片头像列后续拍板）；新模块 icons.js | IM UI v2 会话 | im/index.js、im/src/pages/auth.js、im/src/api/{convs,auth}.js、im/src/mail.js、im/public/assets/**（css/js 全量重写+icons.js 新增）、docs/IM-UI改版方案.md（新建）、docs/IM聊天方案.md（§5/§6/§12 增量）、docs/上线记录.md、本看板 | 无独占（push 走 CI 自动部署；本地 wrangler dev 从 im/ 目录单实例自管） | ✅ 完成：本地 v2 脚本 13 断言全过（peer_last_read/read 帧/D1 推进/重复读不重播）+ esbuild bundle + dry-run + check-i18n 全绿 + 真浏览器全流程（注册态三栏/添加好友/私聊互发/✓→✓✓ 实时翻转/群信息抽屉/资料保存/搜索 chips 过滤/移动端 720px 两态）+ 10 项反馈逐项核验 ✅；顺修 4 bug（轮询打断表单、ctx 缺 setter、[hidden] 被压、M3 遗留 DEL 未导入）；CI 部署见上线记录 |
| 2026-09-14 | IM 业主截图报修三项：① **dm 建会话竞态→幽灵会话钥**（openDm 以本地列表判「未建过」+ 服务端信封先到先得：后建方信封被静默弃但前端 cacheKit 照存自造 K → 该端此后发出的消息对端恒显「[无法解密的消息]」、对端发送方自己看正常、刷新页面自愈；修=dmCreate 信封写入改批内原子 NOT EXISTS 守卫并响应 keys_written，客户端仅在真写入时 cacheKit 否则弃缓存走信封解包重建）② 空态「个人资料」按钮误用齿轮图标（⚙=设置的既定语义冲突）→ 新增单人社 user 图标替换 ③ 列表面板 ⋮ 菜单（设置/退出登录，与 ⚙ 设置重复）改「批量选择会话/群聊」模式 + .im-p-head 补 position:relative（原 .im-menu 绝对定位锚到远祖先致菜单飞到窗口右上）——③ 批量操作项业主已拍板四项（删除/标已读/置顶/免打扰）并实施：迁移改用 **008**（007 归行 41，两行迁移均已先行执行远程 D1） | IM 报修会话 | im/src/api/convs.js、im/public/assets/js/{app.js,convlist.js,icons.js}、im/public/assets/css/im.css（文件尾追加）、scripts/migrations/008-20260914-im-member-prefs.sql、im/src/do/room.js、im/index.js、docs/上线记录.md、本看板 | 无独占（push 走 CI 自动部署） | ✅ 完成：①② commit 1e2098d（CI 34815949127）+ ③ commit b427ee6（CI 34820021791）——本地 API 脚本 27 断言 + 真浏览器冒烟 15 断言（截图 .build-tmp/im-sel-*.png）+ bundle/dry-run/check-i18n 全绿；线上核验 convlist.js 含 im-selbar、/state 未登录 401；与行 41 会话同文件错位协作（其 P1 commit 5eae76b 已剥我方 hunk，我方 commit 携其气泡尾鳍 im.css hunk 与看板/上线记录 2 行，随库注明）；上线记录已补记 |
| 2026-09-14 | IM 在线判定与实时同步改造（业主报修「登录仍显示离线、必须打开对话才在线 + 列表/联系人同步延迟大」，性能调查→三阶段，业主已拍板）：P0 先行（app.js 切回页面/聚焦/联网立即对齐 + 预览缓存按 seq 失效，消除每 20s 全量重解密与「…」闪烁）；P1 用户级在线（UserPresence DO + /app 启动常驻 /ws 心跳 30s + im_presence 表（007 迁移）读时按 90s 窗口判在线，登录即在线，聊天头部/群副标改读全局在线）；P2 轻量 activity 通知（业主选 A 方案：IMRoom 广播经成员枢纽发 {t:'activity',conv,seq}，前端节流 refreshConvs 回源；好友/群事件发 {t:'contact'}；打开中会话仍走原房间 WS 职责不重叠）；P3（列表在线点/标题徽标等）业主拍板暂不做 | IM 性能方案会话 | im/public/assets/js/app.js（**仅 boot 轮询段与 refreshConvs 两处增量，与行 40 ③ 批量模式无行交集，行 40 动 app.js 前请先 rebase**）、P1：im/src/{ws.js,config.js,do/room.js,api/auth.js}、im/src/do/presence.js（新增）、scripts/migrations/007-20260914-im-presence.sql（**007 归本行**；行 40 若拍板需服务端迁移请用 008 并在此行注记）、im/index.js、docs/IM在线与实时同步方案.md（新建）、docs/IM聊天方案.md（§ 增量）、docs/上线记录.md、本看板 | 无独占（push 走 CI 自动部署；本地 wrangler dev 从 im/ 目录单实例自管） | ✅ P0+P1 已上线（66a18b6 run 34816346166 / 5eae76b run 34819854992，迁移 007 远程已执行，上线记录已补记）；✅ P2-A 已实施（activity/contact 轻量推送，p2-test 12 断言+真浏览器徽标无刷新 2→3 验证；随本行收尾 commit 上线，上线记录补记） |
| 2026-09-14 | IM 移动端 WhatsApp 化 v2.1（业主 3 项拍板：主题保持焰橙 D1/底部 4 tab 收敛/聊天 tab 未读数字胶囊，方案 docs/IM-UI改版方案.md §8 已落）：列表态头部两段式（左 ⋯ 菜单 + 右 ✎ 实心 brand 圆钮 + 「焰境密语」大标题 ~30px）+ 会话行 WhatsApp 化（头像 54px/未读行名字时间加粗/dm 末条预览 ✓✓（peer_last_read 纯前端）/未读徽标挪 --unread 变量）+ rows 底部常驻 E2E 提示（icons.js +lock）+ 聊天态发送键有字变实心 accent + 底部 5 钮收敛 4 tab（聊天/联系人/设置/我，图标+11px 标签+激活胶囊；rail 退出钮移除，个人资料页 pf-logout 已有入口）+ 聊天 tab 未读数字胶囊（unread 前端求和、99+ 封顶）+ 列表↔聊天推拉动效（prefers-reduced-motion 关）；后端零改动、桌面 ≥721px 不动；滚动收缩大标题/右滑返回列 v1.3 可选 | IM 移动端 v2.1 会话 | im/public/assets/css/im.css、im/public/assets/js/{convlist,app,chat,icons}.js、im/src/pages/auth.js、docs/IM-UI改版方案.md（§8 本轮已落）、docs/上线记录.md（收尾）、本看板 | 无独占（push 走 CI 自动部署）；**前置=行 38 热修与行 40 报修会话均 ✅ 落 commit 再开工**（im.css/convlist.js/app.js/icons.js 四文件重叠）；行 42 性能会话动 app.js（boot 轮询段）开工前重读磁盘现状、错位合并 | ✅ 完成（方案 §8.7 实施补记）：实施 6 文件（im.css、convlist.js、app.js、icons.js、auth.js 壳、**chat.js 顺修发送钮初始 disabled 补空输入口径**）+ 顺修行 40 遗留（批量操作钮空选 `every` 恒真文案翻转，补 length 守卫）；业主预览两轮反馈当日修订：①动画拍板统一=底部 tab 间直接切换、滑动只留层级导航（列表→聊天详情 push/返回 pop，`body.im-slide` 门控 transition）②底部 tab 改 WhatsApp 式圆角浮岛（左右 10px+safe-area 悬浮/26px 圆角/描边阴影/`display:contents` 4 tab 等距）；真浏览器 390px+1280px 双视口、双账号全链路（互加好友→dm→互发：行未读加粗+徽标、tab 未读胶囊实时亮、✓✓ read 帧翻转、发送钮空灰有字实心）+ tab 级瞬切/层级 push 帧采样全验 + console 零报错；esbuild bundle + dry-run + check-i18n 全绿；测试账号仅本地 D1；部署=push 走 CI 自动部署 |
| 2026-09-14 | 气泡尾鳍平滑化（业主报修「小尾巴生硬」）：border 三角（8×9 硬拼）与气泡圆角衔接处有豁口、轮廓锐利 → 改 clip-path 弧形尾鳍（外弧圆肩+内弧凹收+软尖，20×15 盒 right/left:-8px，z-index:-1 垫到气泡背景之下防压时间/回执文字；@supports 保底旧三角）；仅动 im.css 气泡段（394-437 行区），**与行 40 在飞的 im.css 尾部批量选择段无行交集——im.css 有他人在飞未提交改动，本次用 update-index 外科手术式只暂存气泡 hunk，禁整文件 add** | 尾鳍热修会话 | im/public/assets/css/im.css（仅气泡段）、本看板、docs/上线记录.md | 无独占（push 走 CI 自动部署；本地 wrangler dev 从 im/ 目录单实例自管） | ✅ 完成：clip-path 弧形尾鳍上线（首版内凹收尾与底边有豁口，放大目检后改 Telegram 式贴底外扩弧、豁口消零）；静态隔离页双侧放大目检（豁口 0、时间/✓✓ 不被压、群聊 theirs.grp 同样式生效）；@supports 保底旧三角；check-i18n 全绿；update-index 外科手术式暂存（staged diff 恰 20 行 @supports 块，行 40 尾部批量选择段零夹带）；改动随行 40 会话 b427ee6 携库上线（归属注记见上线记录），CI run 34820021791 success、线上 im.css 含 clip-path ×3 ✅ |

## 待认领

| 挂出 | 事项 | 验收标准 |
|---|---|---|
| 2026-09-10 | **push 全组被拒：GitHub 单文件 100MB 硬限**——批次二a 提交 91ef04f 含 246MB `game/r2-assets/g/4399-230326/…/Build/WEB_4399_1.21.dat`（死神VS火影 Build 包），pre-receive hook declined（`remote rejected`）；本地未推提交堆积 91ef04f / 5f77bb7 / a6cc678 三个。**业主拍板：留给批次二a 自行处理**（外链扩展会话诊断：R2 桶已验证有完整副本——线上 `/g/4399-230326/…/WEB_4399_1.21.dat` 返回 200 全量 257,913,065 字节，磁盘副本保留，移出版本库对线上零影响；另 4399-212029 的 97.4MB unityweb blob 低于限额可推，是否顺带出库由批次二a 自定） | 批次二a 改写/filter 移除 >100MB blob 后 `git push` 全绿（origin/main 追平本地，含 a6cc678 发布）；素材以 R2 桶 + 磁盘为准，比赛材料展示路径不受影响 → **已由同日仓库瘦身任务根治**：r2-assets 全量历史剥离 + force push，push 恒不再携带大 blob（见「进行中」瘦身行） |
| 2026-09-10 | 密室逃脱3 zh 母本补 deviceNote「Flash 内核（包内 Ruffle）」（采集方案 §3.2 要求，英文版方案 §9.3 挂出） | games.json 该条目落 deviceNote；EN 覆盖表同步镜像；check-i18n 全绿；部署后中英列表页均可见 |
| 2026-09-10 | 游戏站 EN 元数据补录真机人工抽检（业主侧） | 手机过 `/en/`：5 张卡全英文（badge=Web classic）、390px 无横溢；`/en/play/4399-246749/` 顶栏英文标题（英文版方案 §9.5 惯例遗留项） |

## 已完成（留档）

| 日期 | 任务 | 会话 | 交付 |
|---|---|---|---|
| 2026-09-10 | 街机死神 4399-196556 定分辨率适配垫片（fit 缩放+输入重映射+焦点回笼三件套） | 适配垫片会话 | R2 定向重传 entry 3 次（fit 首版 → 点击失灵追加输入重映射 → 全屏"卡死"追加焦点回笼；即时生效，Worker 零改动）；整合方案 §7.8 落档；上线记录已补记；两视口居中偏差 0px、点击进关双端通过、全屏内键盘存活（按 d 位移实测） |
| 2026-09-10 | EN 元数据欠账补录：4399 批次 5 款 EN 化 + win12 过期 deviceNote 修正 + check-i18n 汇总行修复 | EN 补录会话 | 部署 e3d7a731；commit d9ad0c6（数据层随 31ec277 在库）；上线记录已补记 |
| 2026-09-10 | 外链专区扩展（决策⑰）：海外云平台 7 家并入「云端畅玩」+ 12 家商店/分发平台新开「正版平台」紧凑链接分区 | 外链扩展会话 | 方案 00bbf5c（§8.6 入档）+ 实施 020f425；线上经批次二a 部署 71e7dc22 携带生效；生产冒烟 zh/EN 17 断言全过；上线记录已补记 |
| 2026-09-14 | IM 通行密钥（Passkey）登录扩展评估（业主提议→**拍板不做**）：通行密钥在 IM 只能替代「认证」一步、替代不了「恢复 E2EE 私钥」一步（恢复锚=登录密码，服务器无私钥帮不上忙）；新设备 passkey 登录后仍需一次密码解包 enc_priv_key，免密不彻底；且 IM 已有验证码这条无密码登录路径、会话 7 天长效，收益仅限同浏览器重登省一次密码，与 ~250 行改动+新增边界解释不划算。若将来做：复用 shared/webauthn.js + webauthn_credentials(portal='im') 零迁移，新设备复用 im-rec 解锁流 | 密钥登录方案会话 | 无代码改动；评估过程与完整方案在会话记录；本行即留档 |

## 冲突事故复盘

| 日期 | 事故 | 根因 | 改进 |
|---|---|---|---|
| 2026-09-10 | 批次二a 会话清扫 game/scripts 产物时 `/_` 通配误删 win12 三工具（_win12-lang-clean/_win12-probe/_win12-verify），两个提交后自察还原（bdcee56） | 根因①清扫用 `git ls-files | grep /_` 通配把「下划线开头的他人常驻工具」误判为产物；根因②自愈循环产物直接写在工具目录里、且误提交入库，才导致这场清扫-误伤-还原连锁 | 改进：产物统一落 `.build-tmp/game/`（.gitignore 已配），清扫清单必须逐项人工过目不用通配；管线工具（含下划线前缀）已全部正式入库 |
| 2026-09-10 | 91ef04f 含 245.97MB 超限 blob 致 push 拒收、远端停 020f425（多会话连带） | 批次二a 会话把 R2 交付的大件（死神 .dat 245.97MB）直接入了 git；GitHub 硬限 100MB | filter-branch 重写未推 5 提交剔 blob（新链尾 23d8335）+ .gitignore 排除两大件（磁盘/R2 保留即交付）+ push 成功；改进：**入 git 前先查 >50MB 文件**，大件策略=「R2 即交付、git 不承载」，已入 .gitignore |

| 2026-09-10 | **push 全线被堵（进行中）**：91ef04f（批次二a r2-assets 17 包入库）含 `4399-230326/.../Build/WEB_4399_1.21.dat` **245.97MB**，超 GitHub 100MB 硬限，pre-receive 拒收 → 远端停在 020f425，其后 4 个提交（5f77bb7/91ef04f/37ed4ae/d8df81d）全部无法发布；外链扩展会话 a6cc678 的「一并发布」实际同样被拒 | 批次二a 提交前未核单文件体积（439MB 大包直接 `git add` 入库）；外链扩展会话 push 被拒后误判成功 | **修复步骤（归属批次二a）**：① `git rm --cached "game/r2-assets/g/4399-230326/szhong.4399.com/4399swf/upload_swf/ftp53/huangcijin/20260202/08/Build/WEB_4399_1.21.dat"`（文件留在磁盘/R2，git 不再追踪）+ `git commit --amend --no-edit`；② `git rebase --onto <新91ef04f> 91ef04f` 顶上后两个 docs 提交（或 `git pull --rebase` 前先本地重放）；③ push 后在本行补记新提交号。其余会话暂勿 push（会被同一 blob 连坐拒绝）；97.44MB 的 WebGL_Mota3D.data.unityweb 仅警告不拦，可后续再议 LFS |
| 2026-09-10 | 象棋提交 31ec277 整文件携带并行会话的 games.en.json 改动（被迫携带式提交） | 已部署内容长期不落 commit，提交窗口跨会话 | CLAUDE.md 节奏铁律：部署后 30 分钟内 commit + push，只 add 本任务文件 |
| 2026-09-10 | 批次二c：剩余 4399 款（~82）滚动自愈+zh/en 登记上架（承批次二a，运行时自愈器逐款实跑收 404 回源补抓） | 批次二c 会话（=批次二a 续） | games.json、games.en.json、game/r2-assets/g/4399-*/ 增量、上线记录 | R2 上传器（补传自愈增量）+ 游戏站部署（分批窗口，与批次五等协调） | ⏸️ 业主拍板暂缓上架（款量过多、多数不完整）——自愈循环完成 8 款后停机；r2-assets 82 目录已本地语义化重命名（_rename_map.txt 留档），R2 键与 games.json 未动；重启时按新名走「自愈→登记」即可 |
